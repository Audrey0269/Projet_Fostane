{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ category.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_index') }}"><button class="bg-dark text-white rounded-4">Retour</button></a>
    <a href="{{ path('app_category_edit', {'id': category.id}) }}"><button class="bg-dark text-white rounded-4">Modifier</button></a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
