{# __________Lien pour icone FA_______________ #}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css">


    <nav class="navbar navbar-expand-lg bg-danger">
        <div class="container-fluid">
            {# <a class="navbar-brand" href="#">Navbar</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button> #}
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link active text-black fw-bolder text-uppercase" aria-current="page" href="{{ path('app_home') }}">Home</a>
                    </li>


                    {% if app.user %}

                    <li class="nav-item">
                        <a class="nav-link text-black fw-bolder text-uppercase" href="{{ path('app_logout') }}">Déconnexion</a>
                    </li>

                    {#
                    <li class="nav-item">
                        <a class="nav-link text-black fw-bolder text-uppercase" href="{{ path('app_panier') }}"><i class="fa-solid fa-basket-shopping"></i></a>
                    </li> #}
                    <li class="nav-item">
                        <a class="nav-link text-black fw-bolder text-uppercase" href="{{ path('app_compte') }}">Mon compte</a>
                    </li>



                        {% if is_granted('ROLE_ADMIN') %}


                        <li class="nav-item">
                            <a class="nav-link text-black fw-bolder text-uppercase" href="{{ path('app_category_index') }}">Categorie</a>
                         </li>
                          <li class="nav-item">
                            <a class="nav-link text-black fw-bolder text-uppercase" href="{{ path('app_product_index') }}">Produit</a>
                         </li>
                        {#<li class="nav-item">
                            <a class="nav-link text-black fw-bolder text-uppercase" href="{{ path('app_tva_index') }}">TVA</a>
                        </li>#}
                        {#<li class="nav-item">
                        <a class="nav-link text-black fw-bolder text-uppercase" href="{{ path('app_carousel_index') }}">Carousel</a>
                        </li>#}

                        <li class="nav-item">
                            <a class="nav-link text-black fw-bolder text-uppercase" href="{{ path('app_commande_index') }}">Commande</a>
                        </li>

                        {% endif %}


                        {% else %}
                         <li class="nav-item">
                            <a class="nav-link text-black fw-bolder text-uppercase" href="{{ path('app_login') }}">Connexion</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-black fw-bolder text-uppercase" href="{{ path('app_register') }}">Inscription</a>
                        </li>
                        {% endif %}

                </ul>
            </div>
        </div>
    </nav>