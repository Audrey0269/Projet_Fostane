{% extends 'base.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}


<div class="container">
    <div>
        <h1 class="text-center">Commandes</h1>


        {% if commande.livraison == false %}
        <a class="btn btn-danger" href="{{ path('app_commande_modif_livraison' , {'id' : commande.id } )}}">Livraison</a>
        {% endif %}

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ commande.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ commande.name }}</td>
                </tr>
                <tr>
                    <th>Adresse</th>
                    <td>{{ commande.adresse }}</td>
                </tr>
                <tr>
                    <th>Date_commande</th>
                    <td>{{ commande.dateCommande ? commande.dateCommande|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
                <tr>
                    <th>Total</th>
                    <td>{{ commande.total }}</td>
                </tr>
                <tr>
                    <th>Livraison</th>
                    <td>{{ commande.livraison ? 'Yes' : 'No' }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_commande_index') }}">back to list</a>

        {# <a href="{{ path('app_commande_edit', {'id': commande.id}) }}">edit</a>
        {{ include('commande/_delete_form.html.twig') }} #}

    </div>

    <div>
        <h1 class="text-center">Detail de la commande</h1>

            <table class="table p-4 bg-light border">

                <thead>
                    <tr class="text-center bg-danger border-danger">
                        <th scope ="col">Ref</th>
                        <th scope ="col">Nom</th>
                        <th scope ="col">Quantité</th>
                    </tr>
                </thead>

                <tbody>
                    {% for item in commande.detailCommandes %}
                        <tr>
                            <th scope="row">{{ item.ref }}</th>
                            <td>{{ item.name }}</td>
                            <td>{{ item.quantity }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>

    </div>
</div>

{% endblock %}
