{% extends 'base.html.twig' %}

{% block title %}Tva{% endblock %}

{% block body %}
    <h1>Tva</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tva.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tva.name }}</td>
            </tr>
            <tr>
                <th>Taux</th>
                <td>{{ tva.taux }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tva_index') }}"><button class="bg-dark text-white rounded-4">Retour</button></a>
    <a href="{{ path('app_tva_edit', {'id': tva.id}) }}"><button class="bg-dark text-white rounded-4">Modifier</button></a>

    {{ include('tva/_delete_form.html.twig') }}
{% endblock %}
